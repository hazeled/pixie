CMAKE_MINIMUM_REQUIRED(VERSION 3.18)

PROJECT(pixie_compiler)

SET(CMAKE_EXPORT_COMPILE_COMMANDS ON)
SET(SRCDIR "source/")

FILE(GLOB_RECURSE SRC ${SRCDIR}/*.c)

ADD_EXECUTABLE(pixie_compiler ${SRC})
