#pragma once

// If disabled, dynamic arrays wont check to make sure indexes aren't out of bounds. This will result in slightly faster runtime, but could be more error-prone
#define DYN_ARRAY_LENGTH_CHECKS
